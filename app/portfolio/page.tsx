import { ArrowUpRight } from 'lucide-react'
import Link from 'next/link'
import { CtaBand, MediaSlot, SectionHeading, SiteShell } from '@/components/site'
import { projects } from '@/lib/site-data'
export default function PortfolioPage() { return <SiteShell><main><section className="bg-primary py-24 text-primary-foreground"><div className="container"><p className="eyebrow text-accent">Portfolio</p><h1 className="mt-5 max-w-4xl text-balance text-6xl font-black tracking-[-.08em] md:text-8xl">Work with a point of view.</h1><p className="mt-7 max-w-2xl text-lg leading-8 text-primary-foreground/70">A visual selection of the projects and partnerships that define our practice.</p></div></section><section className="container py-24"><SectionHeading eyebrow="Selected case studies" title="The work speaks in detail."/ ><div className="flex flex-col gap-20">{projects.slice(0, 3).map((project, index) => <article key={project.slug} className="grid gap-8 md:grid-cols-[1.3fr_.7fr] md:items-end"><MediaSlot label={`Portfolio image slot 0${index + 1} · ${project.name}`} tall /><div><p className="eyebrow text-accent">0{index + 1} · {project.status}</p><h2 className="mt-4 text-3xl font-black">{project.name}</h2><p className="mt-4 leading-7 text-muted-foreground">{project.description}</p><Link href={`/projects/${project.slug}`} className="mt-6 inline-flex items-center gap-2 text-xs font-bold uppercase tracking-[.1em]">Read project <ArrowUpRight /></Link></div></article>)}</div></section></main><CtaBand /></SiteShell> }
