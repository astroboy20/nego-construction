'use client'
import { FormEvent, useState } from 'react'
import { Mail, MapPin, Phone } from 'lucide-react'
import { CtaBand, PageHero, SiteShell } from '@/components/site'
export default function ContactPage() { const [sent, setSent] = useState(false); function submit(event: FormEvent<HTMLFormElement>) { event.preventDefault(); setSent(true) } return <SiteShell><main><PageHero eyebrow="Contact" title={"Let's talk about what you are building."} description="Tell us a little about your project and the team will get back to you. This form is a front-end enquiry flow until the final contact workflow is connected."/><section className="container grid gap-16 py-20 md:grid-cols-[1.1fr_.9fr]"><form onSubmit={submit} className="flex flex-col gap-6 border border-border p-7 md:p-10"><div className="grid gap-6 sm:grid-cols-2"><label className="flex flex-col gap-2 text-sm font-bold">Name<input required className="border border-input bg-background px-4 py-3 font-normal outline-none focus:ring-2 focus:ring-ring" /></label><label className="flex flex-col gap-2 text-sm font-bold">Email<input required type="email" className="border border-input bg-background px-4 py-3 font-normal outline-none focus:ring-2 focus:ring-ring" /></label></div><label className="flex flex-col gap-2 text-sm font-bold">Phone<input className="border border-input bg-background px-4 py-3 font-normal outline-none focus:ring-2 focus:ring-ring" /></label><label className="flex flex-col gap-2 text-sm font-bold">Tell us about the project<textarea required rows={6} className="border border-input bg-background px-4 py-3 font-normal outline-none focus:ring-2 focus:ring-ring" /></label><button className="button-primary self-start" type="submit">{sent ? 'Enquiry received' : 'Send enquiry'}</button>{sent && <p className="text-sm text-primary" role="status">Thanks — your enquiry is ready for the NEGO team to review.</p>}</form><aside className="flex flex-col gap-10"><div><p className="eyebrow text-accent">Find us</p><h2 className="mt-4 text-3xl font-black">NEGO Construction Limited</h2><div className="mt-7 flex flex-col gap-5 text-sm text-muted-foreground"><p className="flex gap-3"><MapPin className="shrink-0 text-accent" />2, Gbolahun Lawal Street, off Ashabi Cole, Lateef Jakande Road, CBD Ikeja, Lagos</p><p className="flex gap-3"><Phone className="shrink-0 text-accent" />0909 110 0558 · 0703 656 4734</p><p className="flex gap-3"><Mail className="shrink-0 text-accent" />hello@nego.example</p></div></div><div className="media-slot min-h-72"><MapPin className="text-accent" size={40} /><span className="absolute bottom-4 left-4 text-xs font-bold uppercase tracking-[.12em] text-primary/50">Map embed slot</span></div></aside></section></main><CtaBand /></SiteShell> }
